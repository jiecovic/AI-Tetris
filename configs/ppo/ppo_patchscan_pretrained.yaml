# @package _global_
defaults:
  - /hydra: default
  - /envs@env_train: warmup
  - /rewards@env_train.reward: custom_heuristic_delta_clipped
  - /envs@env_eval: empty
  - /rewards@env_eval.reward: custom_heuristic_delta_clipped
  - /sb3_policies@policy: pretrained
  - /trains@_global_: ppo_eval
  - _self_

log_level: info                      # logging verbosity

run:
  name: ppo_patchscan_pretrained_run # run name (used for output dir)
  seed: 0                            # base RNG seed
  out_root: runs                     # output root directory
  tensorboard: true                  # enable TensorBoard logging
  device: cuda                       # cuda | cpu | auto
  n_envs: 128                         # fewer envs for faster iteration
  vec: dummy                         # subproc | dummy
