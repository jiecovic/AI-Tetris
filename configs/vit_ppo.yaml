# @package _global_
defaults:
  - hydra: default
  - env_bundles: macro_discrete_warmup
  - models: vit_plus_mlp
  - trains: ppo_bc_eval
  - _self_

log_level: info

run:
  name: vit_run
  seed: 0
  out_root: experiments
  tensorboard: true
  device: cuda
  n_envs: 32
  vec: subproc
